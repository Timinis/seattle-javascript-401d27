'use strict';

const transformDictionary = require('../library/transform.js');

describe('Single Reader Module', () => {
  it('transformDictionary should be an object', done => {
    expect(transformDictionary == true).toBe(false);
  });
});
